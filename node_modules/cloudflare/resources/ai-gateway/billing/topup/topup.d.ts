import { APIResource } from "../../../../resource.js";
import * as Core from "../../../../core.js";
import * as ConfigAPI from "./config.js";
import { Config, ConfigCreateParams, ConfigCreateResponse, ConfigDeleteParams, ConfigDeleteResponse, ConfigGetParams, ConfigGetResponse } from "./config.js";
export declare class Topup extends APIResource {
    config: ConfigAPI.Config;
    /**
     * Create a credit top-up via Stripe PaymentIntent for the given account.
     *
     * @example
     * ```ts
     * const topup = await client.aiGateway.billing.topup.create({
     *   account_id: 'account_id',
     *   amount: 5000,
     * });
     * ```
     */
    create(params: TopupCreateParams, options?: Core.RequestOptions): Core.APIPromise<TopupCreateResponse>;
    /**
     * Get the payment processing status of a top-up by its invoice ID.
     *
     * @example
     * ```ts
     * const response =
     *   await client.aiGateway.billing.topup.status({
     *     account_id: 'account_id',
     *     payment_intent_id: 'in_1abc',
     *   });
     * ```
     */
    status(params: TopupStatusParams, options?: Core.RequestOptions): Core.APIPromise<TopupStatusResponse>;
}
export interface TopupCreateResponse {
    /**
     * Stripe PaymentIntent client secret.
     */
    client_secret: string | null;
    /**
     * Whether the user was already onboarded.
     */
    onboarding: boolean;
    /**
     * Stripe invoice ID.
     */
    payment_intent_id: string;
    /**
     * Card brand (visa, mastercard, etc.).
     */
    brand?: string;
    /**
     * Last 4 digits of card.
     */
    last4?: string;
}
export interface TopupStatusResponse {
    payment_intent_id: string;
    status: 'completed' | 'pending';
}
export interface TopupCreateParams {
    /**
     * Path param: Cloudflare account ID.
     */
    account_id: string;
    /**
     * Body param: Top-up amount in cents (min 1000).
     */
    amount: number;
}
export interface TopupStatusParams {
    /**
     * Path param: Cloudflare account ID.
     */
    account_id: string;
    /**
     * Body param: Stripe invoice ID to check status for.
     */
    payment_intent_id: string;
}
export declare namespace Topup {
    export { type TopupCreateResponse as TopupCreateResponse, type TopupStatusResponse as TopupStatusResponse, type TopupCreateParams as TopupCreateParams, type TopupStatusParams as TopupStatusParams, };
    export { Config as Config, type ConfigCreateResponse as ConfigCreateResponse, type ConfigDeleteResponse as ConfigDeleteResponse, type ConfigGetResponse as ConfigGetResponse, type ConfigCreateParams as ConfigCreateParams, type ConfigDeleteParams as ConfigDeleteParams, type ConfigGetParams as ConfigGetParams, };
}
//# sourceMappingURL=topup.d.ts.map