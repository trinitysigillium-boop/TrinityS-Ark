// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../resource.mjs";
import * as SubdomainsAPI from "./subdomains/subdomains.mjs";
import { SubdomainListResponsesSinglePage, Subdomains, } from "./subdomains/subdomains.mjs";
export class EmailSending extends APIResource {
    constructor() {
        super(...arguments);
        this.subdomains = new SubdomainsAPI.Subdomains(this._client);
    }
    /**
     * Send an email
     *
     * @example
     * ```ts
     * const response = await client.emailSending.send({
     *   account_id: 'account_id',
     *   from: 'sender@example.com',
     *   subject: 'Monthly Report',
     *   to: ['recipient@example.com'],
     * });
     * ```
     */
    send(params, options) {
        const { account_id, ...body } = params;
        return this._client.post(`/accounts/${account_id}/email/sending/send`, {
            body,
            ...options,
        })._thenUnwrap((obj) => obj.result);
    }
    /**
     * Send a raw MIME email
     *
     * @example
     * ```ts
     * const response = await client.emailSending.sendRaw({
     *   account_id: 'account_id',
     *   from: 'sender@example.com',
     *   mime_message:
     *     'From: sender@example.com\r\nTo: recipient@example.com\r\nSubject: Hello\r\nContent-Type: text/plain\r\n\r\nHello, World!',
     *   recipients: ['recipient@example.com'],
     * });
     * ```
     */
    sendRaw(params, options) {
        const { account_id, ...body } = params;
        return this._client.post(`/accounts/${account_id}/email/sending/send_raw`, {
            body,
            ...options,
        })._thenUnwrap((obj) => obj.result);
    }
}
EmailSending.Subdomains = Subdomains;
EmailSending.SubdomainListResponsesSinglePage = SubdomainListResponsesSinglePage;
//# sourceMappingURL=email-sending.mjs.map